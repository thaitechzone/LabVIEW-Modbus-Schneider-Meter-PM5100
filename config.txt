# Configuration File for ESP32 Google Sheets Integration
# Copy these values and update them according to your setup

# WiFi Configuration
WIFI_SSID=Your_WiFi_Name_Here
WIFI_PASSWORD=Your_WiFi_Password_Here

# Google Apps Script Web App URL
# Get this URL after deploying your Google Apps Script as a Web App
WEB_APP_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec

# Hardware Pin Configuration
DHT_SENSOR_PIN=4
LED_ALARM_PIN=2
BUTTON_RESET_PIN=0

# Sensor Configuration
DHT_SENSOR_TYPE=DHT22
# Options: DHT11, DHT22, DHT21

# Timing Configuration (in milliseconds)
SENSOR_READ_INTERVAL=5000
DATA_SEND_INTERVAL=30000
THRESHOLD_GET_INTERVAL=300000

# Default Threshold Values (will be overridden by Google Sheets)
DEFAULT_TEMPERATURE_THRESHOLD=35.0
DEFAULT_HUMIDITY_THRESHOLD=70.0

# Data Management Settings
MAX_DATA_ROWS_IN_SHEETS=1000
AUTO_ARCHIVE_ENABLED=true

# Network Configuration
HTTP_TIMEOUT=10000
WIFI_CONNECT_TIMEOUT=20000
MAX_RETRY_ATTEMPTS=3

# Debug Settings
SERIAL_BAUD_RATE=115200
DEBUG_MODE=true
VERBOSE_LOGGING=true

# Chart Configuration
TEMPERATURE_CHART_COLOR=#ff6b6b
HUMIDITY_CHART_COLOR=#4ecdc4
ALARM_CHART_COLOR=#ffa726

# Dashboard Refresh Settings
AUTO_REFRESH_INTERVAL=5
# Interval in minutes for automatic dashboard refresh

# Google Sheets Column Configuration
TIMESTAMP_COLUMN=A
TEMPERATURE_COLUMN=B
HUMIDITY_COLUMN=C
ALARM_STATUS_COLUMN=D

# Alarm Configuration
ALARM_RESET_BUTTON_ENABLED=true
VISUAL_ALARM_ENABLED=true
AUTO_ALARM_RESET_TIME=0
# Set to 0 to disable auto-reset, or time in minutes

# Language Settings
DASHBOARD_LANGUAGE=th
# Options: en (English), th (Thai)

# Additional Features
EXPORT_TO_CSV_ENABLED=true
EMAIL_NOTIFICATIONS_ENABLED=false
LINE_NOTIFY_ENABLED=false

# System Information
PROJECT_NAME=ESP32-Google-Sheets-Dashboard
VERSION=1.0.0
AUTHOR=Your_Name_Here
CREATED_DATE=2024-01-15

# Notes for Configuration:
# 1. Replace all placeholder values with your actual configuration
# 2. Keep sensitive information (WiFi password, API keys) secure
# 3. Test configuration with small intervals during setup
# 4. Increase intervals for production use to avoid quota limits
# 5. Monitor Google Apps Script execution quotas

# Example Arduino Code Configuration:
# const char* ssid = "Your_WiFi_Name_Here";
# const char* password = "Your_WiFi_Password_Here";
# const char* webAppURL = "https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec";
# #define DHT_PIN 4
# #define DHT_TYPE DHT22
# const int LED_ALARM_PIN = 2;
# const unsigned long SENSOR_INTERVAL = 5000;
# const unsigned long SEND_INTERVAL = 30000;